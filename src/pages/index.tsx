import type { NextPage } from "next";
import Head from "next/head";
import { Button } from "@nextui-org/react";

const Home: NextPage = () => {
  return <Button color="gradient">Hey How its going</Button>;
};

export default Home;
